__pycache__/
*.pyc
*.pyo
.env

# Local build artifacts / not needed for the deployed dashboard
*.zip
.agents/
skills-lock.json
.DS_Store
Thumbs.db

# NOTE: pyodide/ is intentionally NOT ignored — it is the offline runtime
# that the deployed index.html needs. Do not add it here.
