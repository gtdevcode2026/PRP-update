__pycache__/
*.pyc
*.pyo
.env

# Local build artifacts / not needed for the deployed dashboard
*.zip
.agents/
skills-lock.json
.DS_Store
Thumbs.db

# Claude Code / tooling artifacts
.playwright-mcp/
.claude/

# NOTE: vendor/ and js/ are intentionally NOT ignored — the deployed
# index.html needs them (vendored libraries + report engine). Do not add
# them here.
